# Core (required)
PySide6>=6.6          # Qt bindings (LGPL)
pyqtgraph>=0.13       # fast 2D slice viewer
numpy>=1.24
scipy>=1.10           # morphology, distance transforms, flood fill
nibabel>=5.0          # NIfTI I/O

# Optional — DICOM series loading
pydicom>=2.4

# Optional — live 3D view (needs a working OpenGL / GPU)
vtk>=9.2
pyvista>=0.43
pyvistaqt>=0.11
